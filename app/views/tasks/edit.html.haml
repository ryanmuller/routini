%h1
  Edit routine

= render "form"

= form_for @task, :html => { :method => :delete } do |f|
  = f.submit "Remove", :class => [ "btn", "danger" ]

%h2
  Task history 

#log-chart{ :style => "width:500px;height:300px;", 

              "data-pts" => @log_json }
